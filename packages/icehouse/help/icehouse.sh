echo -e "
${text_bold}${text_fg_blue}icehouse${text_reset}

${text_bold}DESCRIPTION${text_reset}

  Cold storage made easy.

${text_bold}USAGE${text_reset}

  ${text_dim}\$${text_reset} ${text_bold}icehouse${text_reset} <command> [options]

${text_bold}COMMANDS${text_reset}

  init                      Initialize a new device for backup storage
  backup                    Perform a backup

${text_bold}OPTIONS${text_reset}

  --help, -h                Show this help message
  --debug                   Show debug messages
"
